# Proxy de desarrollo: el frontend (Vite :5173) reenvía /api y /media al backend.
# Debe ser la misma URL donde corre Django (por defecto puerto 8001).
# Si el backend no está levantado, Vite mostrará "http proxy error" / ECONNREFUSED.
# VITE_PROXY_TARGET=http://localhost:8001
