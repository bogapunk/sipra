#!/bin/sh
# OBSOLETO: el proyecto ya no usa PostgreSQL en Docker.
# Use docker-compose.yml / docker-compose.produccion.yml con Microsoft SQL Server (variables DB_*).
echo "Este script era para conectar un contenedor postgres a la red sipra."
echo "SIP-AIF usa solo SQL Server. No ejecute este archivo."
exit 1
